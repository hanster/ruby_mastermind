#!/usr/bin/env ruby -w
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)
require 'mastermind'
display = Mastermind::Display.new
ai = Mastermind::RandomAI.new
game = Mastermind::Game.new(display,ai)
game_loop = Mastermind::GameLoop.new(game)
game_loop.run
